<UserControl x:Class="NTNL.Views.items.TweetColumn"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             xmlns:vm="clr-namespace:NTNL.ViewModels"
             xmlns:v="clr-namespace:NTNL.Views"
             d:DesignHeight="660" d:DesignWidth="1100">

		<ListBox Background="Transparent" ItemsSource="{Binding columnList}">
			<ListBox.ItemTemplate>
				<DataTemplate>
					<StackPanel Orientation="Vertical" Width="250">
						<TextBlock Text="{Binding Title}" Height="40" Width="250" FontSize="24"/>
						<ListView Height="580" Width="250" Background="Transparent" ItemsSource="{Binding tweetList}">
							<ListView.View>
								<GridView>
									<GridViewColumn/>
								</GridView>
							</ListView.View>
						</ListView>

					</StackPanel>

				</DataTemplate>
			</ListBox.ItemTemplate>
			<ListBox.ItemsPanel>
				<ItemsPanelTemplate>
            <WrapPanel/>
        </ItemsPanelTemplate>
			</ListBox.ItemsPanel>
			
		</ListBox>
</UserControl>
